GLANCE_PRIMARY_NODE: "{{ groups['glance_registry_mgmt_ip'][0] }}"
GLANCE_HOST: "{{ control_bond }}"
GLANCE_REGISTRY_HOST: "{{ internal_lb_vip_address }}"
GLANCE_REGISTRY_PREVIOUS_TAG:
GLANCE_REGISTRY_PREVIOUS_NAME:
GLANCE_DIR: "/var/lib/glance/images"
PASSWORD_VARS: ADMIN_USER_PASSWORD,GLANCE_DB_PASSWORD,DB_ROOT_PASSWORD,GLANCE_KEYSTONE_PASSWORD,RABBITMQ_PASSWORD
