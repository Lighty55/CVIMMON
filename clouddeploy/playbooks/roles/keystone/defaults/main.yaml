INIT_DB: "true"
TOKEN_PROVIDER: fernet
TOKEN_DRIVER: sql
KEYSTONE_HOST: "{{ control_bond }}"
KEYSTONE_FERNET_BUILD_DIR: "/var/tmp/keystone-fernet-keys"
KEYSTONE_FERNET_KEYS_REPO: "/etc/keystone/fernet-keys"
KEYSTONE_FERNET_MAX_KEYS: 3
KEYSTONE_TOKEN_EXPIRATION: 14400
KEYSTONE_PREVIOUS_TAG:
KEYSTONE_PREVIOUS_NAME:
_list_all_keys:
  stdout_lines: []
PASSWORD_VARS: ADMIN_USER_PASSWORD,DB_ROOT_PASSWORD,RABBITMQ_PASSWORD,KEYSTONE_DB_PASSWORD
