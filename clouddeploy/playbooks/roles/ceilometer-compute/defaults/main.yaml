CEILOMETER_HOST: "{{ control_bond }}"
KEYSTONE_REGION: RegionOne

# VOLUMES TO BE MOUNTED FOR LIBVIRT
LIBVIRT_VOLUME:  "/var/lib/libvirt"
QEMU_VOLUME: "/etc/libvirt/qemu"
RUN_VOLUME: "/run"
CGROUP_VOLUME: "/sys/fs/cgroup"
CEILOMETER_COMPUTE_PREVIOUS_TAG:
CEILOMETER_COMPUTE_PREVIOUS_NAME:
PASSWORD_VARS: ADMIN_USER_PASSWORD,CEILOMETER_DB_PASSWORD,DB_ROOT_PASSWORD,CEILOMETER_KEYSTONE_PASSWORD,RABBITMQ_PASSWORD
