HEAT_HOST: "{{ control_bond }}"
HEAT_PRIMARY_NODE: "{{ groups['heat_engine_mgmt_ip'][0] }}"
HEAT_ENGINE_PREVIOUS_TAG:
HEAT_ENGINE_PREVIOUS_NAME:
PASSWORD_VARS: ADMIN_USER_PASSWORD,HEAT_DB_PASSWORD,DB_ROOT_PASSWORD,HEAT_KEYSTONE_PASSWORD,HEAT_STACK_DOMAIN_ADMIN_PASSWORD,RABBITMQ_PASSWORD
